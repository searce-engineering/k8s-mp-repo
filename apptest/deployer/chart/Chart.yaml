apiVersion: v2
name: byol-python-app
# Must match the main chart name exactly — the deployer overlays
# this chart's templates on top of the main chart's resources.
description: Verification tester for byol-python-app
type: application
version: 1.0.0
appVersion: "1.0.0"