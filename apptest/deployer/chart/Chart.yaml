apiVersion: v2
name: gcp-mp-poc-k8s
# Must match the main chart name exactly — the deployer overlays
# this chart's templates on top of the main chart's resources.
description: Verification tester for gcp-mp-poc-k8s
type: application
version: 1.0.0
appVersion: "1.0.0"